import { GetParams } from "@/interfaces/urlParams";
import React from "react";

export default function RolePermissionEdit(params: GetParams) {
  console.log(params.params.permissionSet);
  return <div>page</div>;
}
